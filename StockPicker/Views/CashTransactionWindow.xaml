<Window x:Class="StockPicker.Views.CashTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cash"
        Width="380" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="4,3" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </Window.Resources>

    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="18,6" Margin="0,0,8,0" />
            <Button x:Name="OkButton" Content="OK" Click="Ok_Click" IsDefault="True" Padding="18,6" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="HeaderText"
                       FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,10" TextWrapping="Wrap" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Amount  $" Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="1" Grid.Column="1" x:Name="AmountBox"
                       ToolTip="Dollar amount" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Date"      Style="{StaticResource FieldLabel}" />
            <DatePicker Grid.Row="2" Grid.Column="1" x:Name="DatePickerBox" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Note"      Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="3" Grid.Column="1" x:Name="NoteBox"
                       ToolTip="Optional reason (e.g. paycheck, transfer out)" />
        </Grid>
    </DockPanel>
</Window>
