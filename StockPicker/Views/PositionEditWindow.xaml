<Window x:Class="StockPicker.Views.PositionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Position"
        Width="420" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="4,3" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </Window.Resources>

    <DockPanel Margin="16">
        <!-- Buttons -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="18,6" Margin="0,0,8,0" />
            <Button Content="Save"   Click="Save_Click" IsDefault="True" Padding="18,6" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Symbol"        Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="0" Grid.Column="1" x:Name="SymbolBox" CharacterCasing="Upper"
                       ToolTip="Ticker symbol, e.g. AAPL" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Company"       Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="1" Grid.Column="1" x:Name="CompanyBox"
                       ToolTip="Optional company name" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Entry price"   Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="2" Grid.Column="1" x:Name="EntryPriceBox"
                       ToolTip="Price you actually paid per share" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Shares"        Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="3" Grid.Column="1" x:Name="SharesBox"
                       ToolTip="Number of shares held" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Entry date"    Style="{StaticResource FieldLabel}" />
            <DatePicker Grid.Row="4" Grid.Column="1" x:Name="EntryDatePicker" />

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Planned exit"  Style="{StaticResource FieldLabel}" />
            <DatePicker Grid.Row="5" Grid.Column="1" x:Name="PlannedSellPicker"
                        ToolTip="Optional planned exit date" />

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Holding"       Style="{StaticResource FieldLabel}" />
            <ComboBox  Grid.Row="6" Grid.Column="1" x:Name="HoldingPeriodBox" />

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Source"        Style="{StaticResource FieldLabel}" />
            <TextBox   Grid.Row="7" Grid.Column="1" x:Name="SourceBox"
                       ToolTip="Where this position came from (defaults to Manual)" />

            <TextBlock Grid.Row="8" Grid.Column="0" Text="Notes"         Style="{StaticResource FieldLabel}"
                       VerticalAlignment="Top" Margin="0,3,10,0" />
            <TextBox   Grid.Row="8" Grid.Column="1" x:Name="NotesBox"
                       AcceptsReturn="True" TextWrapping="Wrap" MinHeight="56"
                       VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" />
        </Grid>
    </DockPanel>
</Window>
