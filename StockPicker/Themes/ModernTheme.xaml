<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════════════════════════════════════════════════════════════════════
         Modern flat theme — hand-rolled, zero dependencies.
         Implicit styles restyle the stock WPF controls app-wide (both layouts,
         Settings and dialog windows included) without touching any feature
         wiring. Palette: neutral surfaces, blue accent, softened semantics.
         ══════════════════════════════════════════════════════════════════════ -->

    <!-- ── Palette ── -->
    <Color x:Key="App.BackgroundColor">#F4F6F8</Color>
    <SolidColorBrush x:Key="App.BackgroundBrush"   Color="{StaticResource App.BackgroundColor}" />
    <SolidColorBrush x:Key="App.SurfaceBrush"      Color="#FFFFFF" />
    <SolidColorBrush x:Key="App.SurfaceAltBrush"   Color="#F7F9FB" />
    <SolidColorBrush x:Key="App.AltRowBrush"       Color="#FAFBFD" />
    <SolidColorBrush x:Key="App.BorderBrush"       Color="#E3E7EC" />
    <SolidColorBrush x:Key="App.BorderSoftBrush"   Color="#EDF0F4" />
    <SolidColorBrush x:Key="App.TextBrush"         Color="#1B2430" />
    <SolidColorBrush x:Key="App.TextSecondaryBrush" Color="#5B6472" />
    <SolidColorBrush x:Key="App.TextTertiaryBrush" Color="#8A93A2" />
    <SolidColorBrush x:Key="App.AccentBrush"       Color="#2563EB" />
    <SolidColorBrush x:Key="App.AccentDarkBrush"   Color="#1D4FD7" />
    <SolidColorBrush x:Key="App.AccentSoftBrush"   Color="#E8F0FE" />
    <SolidColorBrush x:Key="App.HoverBrush"        Color="#F1F5FB" />
    <SolidColorBrush x:Key="App.PositiveBrush"     Color="#178744" />
    <SolidColorBrush x:Key="App.NegativeBrush"     Color="#C6314B" />

    <!-- ── Window ── -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource App.BackgroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI, Arial" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <!-- ── Tab headers: flat with an accent underline indicator ── -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource App.TextSecondaryBrush}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Root" Background="Transparent"
                            Padding="13,7,13,0" Margin="0,0,2,0" CornerRadius="4,4,0,0">
                        <StackPanel>
                            <ContentPresenter x:Name="Content" ContentSource="Header"
                                              HorizontalAlignment="Center" Margin="0,0,0,6"
                                              RecognizesAccessKey="True" />
                            <Rectangle x:Name="Indicator" Height="2.5" RadiusX="1.25" RadiusY="1.25"
                                       Fill="Transparent" Margin="-2,0" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource App.HoverBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Indicator" Property="Fill" Value="{StaticResource App.AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource App.AccentDarkBrush}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Buttons: flat card with hover tint ── -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource App.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="11,5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource App.HoverBrush}" />
                            <Setter TargetName="Root" Property="BorderBrush" Value="#C8D2DF" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource App.AccentSoftBrush}" />
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource App.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Accent (primary-action) variant, opt-in via Style="{StaticResource AccentButton}" -->
    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource App.AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource App.AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource App.AccentDarkBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── DataGrid ── -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource App.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource App.BorderSoftBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource App.BorderSoftBrush}" />
        <Setter Property="RowBackground" Value="{StaticResource App.SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
        <Setter Property="RowHeaderWidth" Value="0" />
        <Setter Property="MinRowHeight" Value="26" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource App.SurfaceAltBrush}" />
        <Setter Property="Foreground" Value="{StaticResource App.TextSecondaryBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="Padding" Value="8,7" />
        <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource App.AccentSoftBrush}" />
                <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ── GroupBox: card look (used by the Details pane) ── -->
    <Style TargetType="GroupBox">
        <Setter Property="Padding" Value="8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ContentPresenter Grid.Row="0" ContentSource="Header"
                                          TextElement.FontWeight="SemiBold"
                                          TextElement.FontSize="13"
                                          TextElement.Foreground="{StaticResource App.TextBrush}"
                                          Margin="2,0,0,6" />
                        <Border Grid.Row="1"
                                Background="{StaticResource App.SurfaceBrush}"
                                BorderBrush="{StaticResource App.BorderBrush}"
                                BorderThickness="1" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Inputs ── -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource App.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource App.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="#C8D2DF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!-- ── ToolTip: card with soft shadow ── -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource App.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource App.BorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource App.TextBrush}" />
        <Setter Property="Padding" Value="12,9" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="6"
                            Padding="{TemplateBinding Padding}"
                            Margin="0,0,8,8"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.18" />
                        </Border.Effect>
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Slim scrollbars ── -->
    <Style x:Key="SlimThumb" TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Root" Background="#C6CDD6" CornerRadius="4" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="#A9B2BF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="9" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SlimThumb}" Margin="1.5" />
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="9" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="False">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource SlimThumb}" Margin="1.5" />
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
